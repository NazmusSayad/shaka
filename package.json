{
  "name": "shaka-dev",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "precommit": "lint-staged && cargo clippy --all-targets --all-features -- -D warnings"
  },
  "lint-staged": {
    "*.rs": "rustfmt --edition 2024"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.3.0"
  }
}
